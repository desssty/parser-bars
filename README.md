# 🤖 Resume Search & Tracking Bot

Асинхронный Telegram-бот для автоматизированного сбора резюме
с платформ **HeadHunter** и **Habr Career**.

## 🛠 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/desssty/parser-bars.git
cd parser-bars
```

### 2. Создание виртуального окружения и установка зависимостей

```bash
python -m venv venv
source venv/bin/activate
```

# Для Windows:

```bash
venv\Scripts\activate
```

```bash
pip install -r requirements.txt
```

### 3. Установка браузеров для парсера (Playwright)

```bash
playwright install chromium
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта и добавьте:

```
BOT*TOKEN=ваш*токен*от*бот_фазера
EXTERNAL_URL=http://your-url-here.com/api
```

### 5. Запуск бота

```bash
python bot.py
```

## 📂 Структура проекта

```bash
bot.py — точка входа и инициализация Telegram-бота
handlers/ — обработчики команд и сообщений
scrapers/ — скрипты парсинга (Playwright)
├─ hh_scraper.py — основной парсер
└─ habr_scraper.py — дополнительный парсер
services/ — логика управления процессами поиска
config.py — конфигурация путей и переменных окружения
```

## 🔒 Безопасность и игнор-лист

Следующие файлы не попадают в репозиторий согласно `.gitignore`:

```bash
.env — конфиденциальные токены
resume.json — сгенерированные отчеты с данными резюме
time.json — файл отслеживания вакансий
**pycache**/ — временные файлы Python
.venv/ — виртуальное окружение
```

## 📄 Дополнительная информация

Подробная документация по расширенным функциям,
асинхронным задачам и форматам данных доступна по пути:

[docs/docs.md](https://github.com/desssty/parser-bars/tree/main/docs/docs.md)
